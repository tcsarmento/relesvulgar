{
  "name": "staticvoid-band-site",
  "version": "1.0.0",
  "description": "Site oficial da banda Static Void",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "preview": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-static-gzip": "^2.1.7"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
